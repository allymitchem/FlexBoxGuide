import React from "react";
import {Navbar, FlexDirection, JustifyContent, AlignContent, AlignItems} from './'

const Main = () => {
  return (
    <div id="main">
    <Navbar/>
    <FlexDirection/>
    <JustifyContent/>
    <AlignContent/>
    <AlignItems/>
    </div>
  );
};

export default Main;
